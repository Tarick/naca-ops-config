---
{{- if .Values.secrets -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "items-worker.fullname" . }}
  labels:
    {{- include "items-worker.labels" . | nindent 4 }}
type: Opaque
data:
{{- range .Values.secrets }}
  {{ .name }}: {{ .value | b64enc | quote }}
{{- end }}
{{- end }}
