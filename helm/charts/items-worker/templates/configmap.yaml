---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "items-worker.fullname" . }}
  labels:
    {{- include "items-worker.labels" . | nindent 4 }}
data:
  config.yaml: |-
{{ tpl (.Files.Get "configs/config.yaml") . |indent 4 }}
